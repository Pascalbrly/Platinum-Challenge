swagger: '2.0'
info:
  description: "Upload the NN model file"
  version: "1.0.0"
  title: "Upload NN Model"
paths:
  /upload_nn:
    post:
      tags:
        - "model"
      summary: "Upload NN model"
      description: "Upload the NN model file"
      consumes:
        - "multipart/form-data"
      produces:
        - "application/json"
      parameters:
        - name: "file"
          in: "formData"
          description: "The NN model file to upload"
          required: true
          type: "file"
      responses:
        200:
          description: "successful operation"
          schema:
            type: "object"
            properties:
              message:
                type: "string"
